<%= simple_form_for(@segment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :strava_url %>
    <%= f.input :description %>
    <%= f.input :length %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
