<%= simple_form_for(@roster) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :join_date %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
